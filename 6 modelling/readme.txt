-- What to do?
Get the model of the target plate

-- Input Arguments
Location of the pcd file

-- Method
Extract clusters. Move the centroid of the each cluster(plate) to the original point using PCA and transform the cluster if the orientation of the cluster(plate) is in a contrary direction.
